class AppConfig {
  // Thay đổi URL này thành URL thật của backend (api-gateway)
  // Nếu chạy device/emulator, lưu ý dùng 10.0.2.2 thay cho localhost
  static const String baseUrl = 'http://localhost:3000';
}
